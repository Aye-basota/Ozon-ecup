# STATE — читать целиком перед каждой задачей (лимит ~80 строк)

## Текущий лучший

**`STRONGEST-CURRENT`** (`exp_037`) — **LB public 1.6496571**, wCV **1.74751**:
0.10·CAP + 0.20·UNC + 0.25·DIST + 0.225·`ETX-AVG3` @ `DCW` + 0.225·`SEQ-AVG3` @ `clip289`,
level 2.3293. Было `SEQ-01-MIX` 1.6501764 (δ **−0.0005193**, 2.1 парной SE), до него `S1-DIST-MIX` 1.6507774.
Честный LOFO **−0.00092 4/4** — рекорд слота SEQ (был −0.00061). **Перенос wCV→LB подтверждён третий раз:
0.564 против ожидавшихся 0.57; прогноз отчёта 1.64966 против факта 1.6496571 — промах 3e-6.**
`SEQAVG3-MIX` full365 = 1.6553136 (`exp_027`); `SEQ-D3A` подтверждён (`exp_030c`), но в смесь не идёт —
ничья с `SEQ-AVG3` при sd 0.00027 (`exp_035`), и тестовых моделей у него нет.
Неотправленный резерв без ETX: `submission_SEQAVG3_clip289_mix.csv` (`exp_035`), LOFO −0.00055 4/4.

## Метрика и валидация — ЕДИНАЯ схема, `exp_016`

Метрика — RMSLE в `log1p`; главная **wCV**: пофолдовая калибровка, веса 1:2:4:8 (`exp_016`;
**перенос SEQ-члена на LB 0.57 — теперь ДВЕ точки: 0.57 `exp_025` и 0.564 `exp_037`**). Фолды 09-04/09-18/10-02/10-16; val-панель 3-блочная,
train-коридор 04-03..10-16, шаг 7, `T+30≤V`. **Пороги:** Δ≤−0.0020 → отправлять;
−0.0020…−0.0005 → разработка; выше — шум; ≥3/4, включая 10-16. Public noise 0.00025,
пол 0.0005. Веса — LOFO, E03a ненулевой. Seed std: GBDT 0.00010, **TCN 0.00250**; пол `Var(Δ)=0.00712`.

## Не повторять (провалы; список только растёт, ничего не удалять)

- **Cutoff 2026-01-14 и любой T≥2025-10-17** отравлены окном панели (`e08`, `exp_028`): P(активность 30д) = **1.0** на 11-15/12-15/01-14 против 0.89–0.93 на чистых. 2-фолдовая схема team-b **отклонена** (`SEQ5/VALIDATION_MAIN_AUDIT.md`).
- **3-блочная train-панель** (`S1-E01`) хуже +0.00102, 4/4; val-панель обязана быть 3-блочной. **×1.16** (`e05`), **user_id** (`e04`), **недельные лаги** (`e15`), **минус длинные фичи** (`S1-E04`) — ноль/хуже; **L=90** (`E03c`) и **строгий L=180** (`E03b`): выбор L по минимуму bias даёт худшую модель.
- **EXP-MIN** (+0.01614) и **EXP-SIM** (+0.01694): низкая corr прогнозов ≠ полезная декорреляция ошибок. **«Асимметрия 0.64×»** отменена (`exp_016`): артефакт OOF cal.
- **Дельты со слабой базы не переносятся**: `DIST` −0.0040 против 3 cutoff'ов и −0.0015 против плотной. **Только выборка последнего fold** (`DIST-F4`): LB +0.00042; локальный валидатор слеп.
- **Rounds≥450 у direct** (`exp_017`): 600 переобучает, 1600 стоят +0.00287, 0/4. **Подбор весов ради 4-го знака** (`MIX-E11`): wCV −0.00038, LB +0.00023; E03a нельзя обнулять.
- **Gap-axis как selection gate** (`exp_019`): bias slope не воспроизведён, зависит от k — только диагностика. **`train_blocks=0`** (`exp_020`): avg3 +0.000113, 2/4. **Вся постобработка поверх `ẑ`** (`rmsle_diagnostics`): аффинная −0.00027, посегментная −0.00017, правила по `rec_buy` неустойчивы; закрыто с `EXP-17`/`EXP-18`.
- **Личное время как представление** (`exp_021`, S_08): 30 признаков `pt_*` → −0.000006 wCV, ΔAUC ≈ 0; контроль с ρ, **перемешанной между людьми**, не хуже настоящей. **Dense supervision при равном объёме** (`exp_022`, S_02B): +0.001263 wCV, 0/4; AUC −0.000416.
- **Multi-horizon hazard + count** (`exp_024`): +0.00286, 0/4; `FULL` 1.75234 = контроль `SELF` 1.75235. **Стекинг мерить от `SELF`: приём сам стоит +0.0004 wCV.**
- **TCN как источник РАНЖИРОВАНИЯ** (`exp_025`/`exp_026`): усреднение сидов не поднимает AUC(y>0) выше смеси; энкодер ценен **только** как член смеси. То же у `ETX`: wCV лучше всех, AUC 0.84135 против 0.84159 у `SEQ-AVG3` (`exp_037`).
- **Полная глубина 365 на тесте у TCN** (`exp_027`, LB +0.0051): у всех 29 обучающих cutoff'ов ≥76 позиций
  `avail=0`, на тесте **0** — непрожитый режим ценой **+0.0034…+0.0044**. **На тесте всегда `--depth-clip 289`**
  (и у `ETX`, вместе с `DCW`). **OOF/LOFO = прогноз С ОБРЕЗКОЙ.**
- **Train-time `avail` augmentation** (`exp_029`): сняли `avail≡1` penalty (+0.00651 → ≈0), но дали +0.00178/+0.00105 RMSLE; gain +77д сжался −0.00308 → −0.00165/−0.00206. Production = `clip289`.
- **Гибрид «табличный экстенсив × интенсив SEQ»** (`exp_032b`): solo −0.00247 4/4, но в смеси **0/4** в слоте SEQ
  (`Var(z−z_tab)` 0.041→0.015). **Лучший одиночный скор ≠ лучший член.** Интенсив хуже `μ_DIST` на +0.00257, 0/4.
- **Новый sequence-член в смесь без проверки РЕЖИМА на тестовом cutoff'е** (`exp_036`): у `ETX`
  `Var(z−z_SEQ-AVG3)` 0.088 на тесте против 0.027 на OOF (**3.22×**), рост МОНОТОННЫЙ с активностью
  (0.48×→**5.70×**), все пары без ETX 0.63–1.11×. **Перед сборкой сверять `Var(Δz)` пары на тесте с её же
  значением на OOF; вне 0.6–1.2× = блокер.** Правило поймало НЕ архитектуру, а баг инференса (`exp_037`) —
  после починки 0.78×, сабмит собран. Гейт остаётся обязательным.
- **Обрезка входа, согласованная не по ВСЕМ каналам** (`exp_037`; класс `exp_027`, +0.0051 LB): `--depth-clip`
  у `ETX` резал окно, а `depth` в query остался календарным (365 на тесте против 93..289 в обучении). Плюс
  `sin/cos(cdow)`: все cutoff'ы — четверги, тест — пятница. **Вход, КОНСТАНТНЫЙ в обучении, — не признак, а
  мина.** Цена на размеченных фолдах: глубина +0.0004…+0.0038 (8 пар из 8, 3 чекпойнта), `dow`+1
  +0.0026…+0.0087. Боевая политика теста ETX — `DCW` (`depth_cap 289` + `cdow` четверг).
- **Разнообразие мерить ДО ТАБЛИЦЫ — неверная ось** (`exp_036`): у `ETX` `Var(z−z_tab)` 0.0263 против 0.0291 у
  `SEQ-AVG3`, то есть ХУЖЕ, а в паре с ним он даёт лучший член слота (−0.00091 4/4). Ценно расстояние до того,
  **кого дополняешь** (`Var(z_ETX−z_SEQ)`=0.033 против 0.008 между сидами TCN), а не до самого далёкого члена.

## Последние эксперименты (макс 10 строк, переполнение → HISTORY.md)

| ID | Дата | Автор | Гипотеза | wCV | Вердикт |
|----|------|-------|----------|-----|---------|
| STRONGEST-CURRENT | 2026-08-20 | A1 | EXP-037: слот `0.5·ETX-AVG3 + 0.5·SEQ-AVG3`, режим теста после фикса статика | **1.74751** | **ОТПРАВЛЕН, LB 1.6496571 — новый чемпион** (`exp_037`) |
| ETX-AVG3 | 2026-08-20 | A1 | EXP-037: 3 сида × 4 фолда + причина блокера `exp_036` | **1.74861** | **ACCEPT как соавтор** (`exp_037`) |
| ETX-01-S42 | 2026-08-20 | A1 | S_13: sparse event transformer, 4 фолда + LOFO слота | **1.74953** (ΔwCV −0.00331) | **пара с TCN −0.00091 4/4** (`exp_036`) |
| MIX9-SEL | 2026-08-19 | A1 | LOFO слота SEQ: `D3A-AVG3` против `SEQ-AVG3 @ clip289` | −0.00061 / −0.00055 | **выбран `SEQ-AVG3`** (`exp_035`) |
| S04-PROD-FRESH | 2026-08-19 | A1 | EXP-032B: боевой экстенсив `1−p0` из `S1-DIST` под тот же `μ_FRESH` | −0.00101 / −0.00247 (гр. A) | **PASS гейта, в смесь НЕ годится** (`exp_032b`) |
| SEQ-D3A-MS | 2026-08-19 | A1 | EXP-030: 3 сида × 4 фолда, парные дельты | ΔwCV **−0.00095** | **KEEP в смесь** (`exp_030c`) |
| S04-SEQ-FRESH | 2026-08-19 | A1 | EXP-032: интенсивная голова на CLEAN+EXTRA, frozen энкодер | −0.00128 (гр. A) | **CONTINUE** (`exp_032`) |
| SEQ-D3A-S43 | 2026-08-19 | A1 | EXP-030b: воспроизводится ли провал 09-18 (сид 43) | 09-18 −0.00035 | **SCALE TO 3 SEEDS** (`exp_030b`) |
| SEQ-D3A | 2026-08-18 | A1 | EXP-030: depth curriculum, обрезка РЕАЛЬНЫХ дней | **1.75284** | **CONTINUE** 2/4 (`exp_030`) |
| SEQ-AVAIL-AUG | 2026-08-14 | A1 | train-only сдвиг границы `avail`, включая `avail≡1` | F4 +0.00105 best | **REJECT** (`exp_029`) |

LB: **STRONGEST-CURRENT 1.6496571** < SEQ-01-MIX 1.6501764 < DIST-MIX 1.6507774 < MIX-E11 1.6510029 <
f4 1.6512012 < S1-BEST 1.6512803 < **SEQAVG3-MIX 1.6553136** < S2 1.6619325 < MIN 1.6674246 <
SIM 1.6682180. `L*=2.3293`.

## Backlog — см. `PROBABLY_EXP.md` и `experiments/README.md`

- [x] **EXP-030** ЗАКРЫТ (`exp_030c`): 3 сида × 4 фолда, ΔwCV −0.00095 ± 0.00009, гейт 3/4 включая 10-16.
- [x] **EXP-032 / EXP-032B** ЗАКРЫТЫ: приём свежести −0.00101…−0.00128 4/4, налог схемы сидел в переобученной голове `P(y>0)` и снимается боевым `1−p0`. Гибрид в смесь не идёт.
- [x] **EXP-035** ЗАКРЫТ: LOFO слота + сабмит; **шум переобучения тестовой модели `Var=0.035` = 72% смены сида**.
- [x] **EXP-036** ЗАКРЫТ (`exp_036`): `ETX` соло = паритет ОДНОЙ модели с трёхсидовым TCN; **как ЗАМЕНА
  REJECT (2/4), как СОАВТОР `0.5·ETX+0.5·SEQ-AVG3` — лучший член слота**. Выигрыш на КРАЯХ активности
  (никогда не покупал ΔAUC +0.0276) и в ДЛИННОЙ истории (на 09-04 соло +0.00005).
- [ ] **EXP-031** `SEQ-D3C` ёмкость TCN; **EXP-033** `S06` Ridge-стек; **EXP-034** `S12` калибровка.
- [x] **EXP-037** ЗАКРЫТ (`exp_037`): блокер `ETX` снят и ОБЪЯСНЁН (два вырожденных входа статика query),
  3.22×→**0.78×**; `ETX-AVG3` wCV **1.74861**, сид-шум sd 0.00009. **Усреднение сидов на OOF не купило ничего**
  (−0.00091→−0.00092), сегментный гейт по `w180_days_buy`/`rec_buy` **REJECT** (α неустойчив по held-out).
- **Decision:** узкое место — **интенсив SEQ** (+0.00257 к `μ_DIST`, 0/4): свежая supervision обязана попасть
  ВНУТРЬ одноголовой модели (low-LR fine-tune на conditional loss). Статусы: `STRATEGIES_INDEX.md`.
