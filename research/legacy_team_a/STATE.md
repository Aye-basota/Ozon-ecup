# STATE — читать целиком перед каждой задачей (лимит ~80 строк)
## Текущий лучший
**`best_public_observed`** — teammate `latest/latest.csv`: **1.6492175622, EXTERNALLY_REPORTED** (LB date UNKNOWN; evidence by 2026-08-24), exact TEST assembly `.12 friend + .16 occ_meta_B + .72 occ_raw_X3` in log-space, source SHA `7ef5b2c5…e722`, reconstruction error `8.88e-16`; `CANONICAL_OOF=MISSING`, `CAP_LINEAGE=UNKNOWN` (`exp_067`).
**`best_exactly_reproducible`** — **`STRONGEST-CURRENT`** (`exp_037`), recorded LB **1.6496571**, wCV **1.74751**, recipe 0.10·CAP + 0.20·UNC + 0.25·DIST + 0.225·ETX-AVG3@DCW + 0.225·SEQ-AVG3@clip289, SHA `abc2218b…e04bda`; **`research_private_safe_anchor` = STRONGEST-CURRENT**, `latest` не CV/LOFO-база.
Честный LOFO **−0.00092 4/4** — рекорд слота SEQ (был −0.00061). **Перенос wCV→LB подтверждён третий раз: 0.564 против ожидавшихся 0.57; прогноз отчёта 1.64966 против факта 1.6496571 — промах 3e-6.**
`SEQAVG3-MIX` full365 = 1.6553136 (`exp_027`); `SEQ-D3A` подтверждён (`exp_030c`), но в смесь не идёт — ничья с `SEQ-AVG3` при sd 0.00027 (`exp_035`), и тестовых моделей у него нет.
Финальный requested blend: **`submission_FINAL_CAP_UNC_DIST_SEQ_ETX_TEAM_B.csv`** (`exp_071`), `.24727 team-b-B2` внутри tab-slot, LOFO **−0.000381 4/4**, regime **1.022× PASS**, SHA `d6cdb218…8b9`; research/private-safe anchor не меняется. Резервы: `submission_LEVEL_MINUS_006.csv` (`exp_060`), `submission_BTYD05.csv` (`exp_051`), `submission_SEQ65_TEMPORAL_HEAVY.csv` (`exp_059`), `submission_SEQAVG3_clip289_mix.csv` (`exp_035`).
## Метрика и валидация — ЕДИНАЯ схема, `exp_016`

Метрика — RMSLE в `log1p`; главная **wCV**: пофолдовая калибровка, веса 1:2:4:8 (`exp_016`; **перенос SEQ-члена на LB 0.57 — теперь ДВЕ точки: 0.57 `exp_025` и 0.564 `exp_037`**). Фолды 09-04/09-18/10-02/10-16; val-панель 3-блочная, train-коридор 04-03..10-16, шаг 7, `T+30≤V`.
**Пороги:** Δ≤−0.0020 → отправлять; −0.0020…−0.0005 → разработка; выше — шум; ≥3/4, включая 10-16. Public noise 0.00025, пол 0.0005. Веса — LOFO, E03a ненулевой. Seed std: GBDT 0.00010, **TCN 0.00250**; пол `Var(Δ)=0.00712`. DET-PAIR execution floor: **0** (`exp_043`), рабочий предел ≤1e-4.
## Не повторять (провалы; список только растёт, ничего не удалять)

- **Cutoff 2026-01-14 и любой T≥2025-10-17** отравлены окном панели (`e08`, `exp_028`): P(активность 30д) = **1.0** на 11-15/12-15/01-14 против 0.89–0.93 на чистых. 2-фолдовая схема team-b **отклонена** (`SEQ5/VALIDATION_MAIN_AUDIT.md`).
- **Full-reference pseudo-selection matching только по `future_blocks_active`** (`exp_048/049`): `π_ref(k=0)=0.004951`, но `k=0` отсутствует во всех 09-04/09-18/10-02 из-за overlap с глобальным selection window. Условный k>0 результат не выдавать за unbiased/matched estimate. Same-fold `BTYD05_FRESH1` **−0.000551, 3/3**, bootstrap/shuffle PASS. Production blocker `exp_050` снят честно в `exp_051`: analytic-jac strict fit одинаково на OOF/test, nested **−0.000269 4/4**, fixed .05 **−0.000321 4/4**, test support PASS, `submission_BTYD05.csv` создан. FRESH exact TEST encoder всё ещё отсутствует; не выдумывать fold averaging/latest-fit/OOF→test carry.
- **3-блочная train-панель** (`S1-E01`) хуже +0.00102, 4/4; val-панель обязана быть 3-блочной. **×1.16** (`e05`), **user_id** (`e04`), **недельные лаги** (`e15`), **минус длинные фичи** (`S1-E04`) — ноль/хуже; **L=90** (`E03c`) и **строгий L=180** (`E03b`): выбор L по минимуму bias даёт худшую модель.
- **EXP-MIN** (+0.01614) и **EXP-SIM** (+0.01694): низкая corr прогнозов ≠ полезная декорреляция ошибок. **«Асимметрия 0.64×»** отменена (`exp_016`): артефакт OOF cal.
- **Дельты со слабой базы не переносятся**: `DIST` −0.0040 против 3 cutoff'ов и −0.0015 против плотной. **Только выборка последнего fold** (`DIST-F4`): LB +0.00042; локальный валидатор слеп.
- **Rounds≥450 у direct** (`exp_017`): 600 переобучает, 1600 стоят +0.00287, 0/4. **Подбор весов ради 4-го знака** (`MIX-E11`): wCV −0.00038, LB +0.00023; E03a нельзя обнулять.
- **TABULAR-BACKBONE-REFRESH** (`exp_046`): exact replay `A=H`; AVG3@600 только **−0.000053, 4/4**, rounds-only **+0.000033, 1/4**, primary AVG3@300 **−0.000002, 3/4**, 10-16 −0.000005. Выигрыш `S1-E10` не переносится внутрь fixed `STRONGEST_CURRENT`; не продолжать rounds/seed tuning и не запускать `DIST-AVG3` из этого результата.
- **Gap-axis как selection gate** (`exp_019`): bias slope не воспроизведён, зависит от k — только диагностика. **`train_blocks=0`** (`exp_020`): avg3 +0.000113, 2/4. **Вся постобработка поверх `ẑ`** (`rmsle_diagnostics`): аффинная −0.00027, посегментная −0.00017, правила по `rec_buy` неустойчивы; закрыто с `EXP-17`/`EXP-18`.
- **Личное время как представление** (`exp_021`, S_08): 30 признаков `pt_*` → −0.000006 wCV, ΔAUC ≈ 0; контроль с ρ, **перемешанной между людьми**, не хуже настоящей. **Dense supervision при равном объёме** (`exp_022`, S_02B): +0.001263 wCV, 0/4; AUC −0.000416.
- **Multi-horizon hazard + count** (`exp_024`): +0.00286, 0/4; `FULL` 1.75234 = контроль `SELF` 1.75235. **Стекинг мерить от `SELF`: приём сам стоит +0.0004 wCV.**
- **Classic common-origin purchase-day BG/NBD как residual member `STRONGEST_CURRENT`** (`exp_047`): nested LOFO **−0.000269, 4/4**, 10-16 −0.000193, веса .05/.05/.10/.10; full fixed curve −0.000324 оптимистична. `P(alive)≈0.994`, `E[N30]≈1.52` против actual 1.91–2.01. **REJECT; закрыть classic BTYD family, не переходить к Pareto/NBD/Gamma-Gamma и не спасать origin/window/K.**
- **TCN как источник РАНЖИРОВАНИЯ** (`exp_025`/`exp_026`): усреднение сидов не поднимает AUC(y>0) выше смеси; энкодер ценен **только** как член смеси. То же у `ETX`: wCV лучше всех, AUC 0.84135 против 0.84159 у `SEQ-AVG3` (`exp_037`).
- **Полная глубина 365 на тесте у TCN** (`exp_027`, LB +0.0051): у всех 29 обучающих cutoff'ов ≥76 позиций `avail=0`, на тесте **0** — непрожитый режим ценой **+0.0034…+0.0044**. **На тесте всегда `--depth-clip 289`** (и у `ETX`, вместе с `DCW`). **OOF/LOFO = прогноз С ОБРЕЗКОЙ.**
- **Train-time `avail` augmentation** (`exp_029`): сняли `avail≡1` penalty (+0.00651 → ≈0), но дали +0.00178/+0.00105 RMSLE; gain +77д сжался −0.00308 → −0.00165/−0.00206. Production = `clip289`.
- **Гибрид «табличный экстенсив × интенсив SEQ»** (`exp_032b`): solo −0.00247 4/4, но в смеси **0/4** в слоте SEQ (`Var(z−z_tab)` 0.041→0.015). **Лучший одиночный скор ≠ лучший член.** Интенсив хуже `μ_DIST` на +0.00257, 0/4.
- **Новый sequence-член в смесь без проверки РЕЖИМА на тестовом cutoff'е** (`exp_036`): у `ETX` `Var(z−z_SEQ-AVG3)` 0.088 на тесте против 0.027 на OOF (**3.22×**), рост МОНОТОННЫЙ с активностью (0.48×→**5.70×**), все пары без ETX 0.63–1.11×. **Перед сборкой сверять `Var(Δz)` пары на тесте с её же значением на OOF; вне 0.6–1.2× = блокер.** Правило поймало НЕ архитектуру, а баг инференса (`exp_037`) — после починки 0.78×, сабмит собран. Гейт остаётся обязательным.
- **Обрезка входа, согласованная не по ВСЕМ каналам** (`exp_037`; класс `exp_027`, +0.0051 LB): `--depth-clip`
  у `ETX` резал окно, а `depth` в query остался календарным (365 на тесте против 93..289 в обучении). Плюс
  `sin/cos(cdow)`: все cutoff'ы — четверги, тест — пятница. **Вход, КОНСТАНТНЫЙ в обучении, — не признак, а
  мина.** Цена на размеченных фолдах: глубина +0.0004…+0.0038 (8 пар из 8, 3 чекпойнта), `dow`+1
  +0.0026…+0.0087. Боевая политика теста ETX — `DCW` (`depth_cap 289` + `cdow` четверг).
- **Разметка будущего ДРУГИМ событием (future funnel Search/Cart) как aux-супервизия sequence-энкодера** (`exp_038`): закрыт КЛАСС. `FUNNEL−BUYCTRL` +0.00106/−0.00008 (λ 0.3/0.1) при цене прогона 0.00033; `rec_buy 15–60` у `buy30` −0.00125, у `FUNNEL` +0.00027. **Обычные отговорки сняты измерением:** метки НЕ дубль покупки (разрыв «своя голова − `z`» 0.05–0.22 против 0.0036 у `buy30`), энкодер их учит (AUC 0.81–0.90) и перестраивается (train MSE 3.1285 против 3.1357 при шуме 0.00015) — прогноз не двигается. **Не пробовать** `to_ord`, `cat`, покатегорийные метки, горизонты >30, `cart→order`, contrastive на будущей активности, предобучение точечным процессом. НЕ закрыто: `buy30` как aux (−0.00122) и **края активности** (funnel-арки ранжируют никогда не покупавших лучше purchase-арок 4/4, +0.005…+0.024 AUC при шуме 0.00085).
- **Block-to-block residual `q·(nu_F−nu_C)` из двух последних guaranteed-active блоков** (`exp_039`, BLOCK4-SAF): **REJECT +0.00024 wCV, 0/4**, 10-16 +0.00021; все held-out α упёрлись в 0.25, дальше вред монотонно растёт. `corr(delta,residual)` −0.004…+0.010; `rec_buy 15–60` +0.00020. Shuffle +0.00740, seed 42/43/44 все дают +0.00031…+0.00036. Test regime честный (Var ratio 0.87), значит дело не в переносе. Не повторять разность соседних conditional-heads как direct correction `STRONGEST_CURRENT`.
- **`COND-FRESH` как residual correction `STRONGEST_CURRENT`** (`exp_040`, FRESH-CONTRAST-MOE): full two-sided cross-fit, nested LOFO **−0.000225, 4/4**, 10-16 −0.000253; VOL +0.000008, A/B −0.000249/−0.000201. `HIGH16` −0.000027, 2/4; HIGH16 standalone signal уже содержится в чемпионе. **Закрыть ensemble-ветку FRESH**, не спасать alpha/сегментами и не переходить автоматически к neural fine-tune.
- **Обучение `src/seq.py` НЕ воспроизводимо между процессами при одном сиде** (`exp_038`): `Batcher.__iter__` гонит 3 потока через общую очередь, порядок шагов SGD — гонка (тест `test_realized_batch_order_is_a_race_above_one_worker`). Цена прогона на 10-16: **0.00033** RMSLE_cal, **0.0204** `Var(Δz)`; внутрипрогонный разброс по эпохам 2–4 = 0.0012–0.0024. **Пара из РАЗНЫХ процессов не строго парная без DET-policy.** `workers=1` + materialized plan + deterministic CUDA дали точные hashes (`exp_043`).
- **Разнообразие мерить ДО ТАБЛИЦЫ — неверная ось** (`exp_036`): у `ETX` `Var(z−z_tab)` 0.0263 против 0.0291 у
  `SEQ-AVG3`, то есть ХУЖЕ, а в паре с ним он даёт лучший член слота (−0.00091 4/4). Ценно расстояние до того,
  **кого дополняешь**, а не до самого далёкого члена. **Поправка `exp_038`:** `Var(z_ETX−z_SEQ)`=0.033 сравнивался
  с 0.008 у ТРЁХСИДОВОГО СРЕДНЕГО (усаженный предиктор, несопоставимая пара); сопоставимые одиночные модели —
  0.02377 там же и 0.0204 у чистого повтора, то есть 0.033 ≈ 1.6 шума прогона. Вердикт `exp_036` (LOFO) не затронут.
- **Ridge на 227 `S1-E10` как фиксированные 15% ансамбля** (`exp_041`): standalone wCV 1.76011, diversity реальна (`Var` к UNC/DIST/STRONGEST 0.060/0.049/0.045), но main/control дали **+0.000278/+0.000207, 0/4**, 10-16 хуже. Не спасать весом, lambda, новыми фичами или другой регрессией.
- **Historical RECENCY-RIDGE replay** (`exp_068`): reference CSV и recipe найдены, но 32/32 OOF и 6/6 helper TEST checkpoints отсутствуют; winner использовал до 72 raw `meta_raw`, а не был чистым prediction-only Ridge. **BLOCKED_HISTORICAL_REPLAY**; не считать inverse/final-CSV roundtrip exact replay, не перефитить lambda/scale и не запускать Phase C до frozen bank.
- **Soft negative correction по amount × `p0_DIST`** (`exp_042`, ZERO2D-SHRINK): honest LOFO **−0.000025, 2/4**, 10-16 −0.000030; AMOUNT-ONLY лучше (−0.000034), shuffled-p0 такой же (−0.000026), actual-zero contribution стал хуже. Hard zero top 5/10% дал +0.00127/+0.00497. Не спасать thresholds, bins, shrinkage, segment weights или hard zeros.
- **Fresh conditional fine-tune plain SEQ-01 против равнообъёмного CLEAN-positive VOL** (`exp_044`): deterministic paired 10-16, seeds 42/43/44, mean **−0.000088**, sd 0.000065, 3/3, но FRESH−baseline +0.000052 и positive-only +0.000295. **REJECT, full folds NO**; auxiliary head учит источник (RMSE −0.00289), direct forecast практически не выигрывает. Не масштабировать эту exact FT-схему на full folds и не переносить вывод на исторический `SEQ-AVG3`/чемпиона.
- **`buy30=1[y30>0]` как auxiliary supervision plain SEQ-01** (`exp_045`, BUYCTRL-DET): строгая deterministic пара против cutoff-wise shuffle, 10-16, seeds 42/43/44 — **FAIL**, `BUYTRUE−BUYSHUF` mean **+0.000436**, median +0.000220, sd 0.000744, лучше 1/3; `BUYTRUE−BASE` +0.000039. Aux честно учится (AUC 0.84589 против 0.53650; BCE 0.46996 против 0.66848), encoder двигается (`Var(Δz)=0.02321`), direct RMSLE не переносит сигнал. Не масштабировать folds и не спасать lambda/epochs.
- **Channel-Shapley monetary decomposition против amount-matched shuffled control** (`exp_052`): pre-flight GO (oracle shape-only −0.48294; past90→future share Spearman 0.2786, 4/4), но pilot 10-16 **REJECT**: REAL хуже SHUF на +0.001266, обе directions выбрали alpha=0, `corr(d,residual)` отрицательна в обеих halves, alpha .25 уже +0.000275. Не запускать full folds и не спасать bins/seeds/rounds/alpha/targets/segment gates.
- **Artifact-only residual winner/gating и signed-residual probes** (`exp_053`): semantic ETX-vs-SEQ oracle +0.020172 ниже seed-null +0.031483 (excess −0.011311); winner AUC 0.526759, но gate 10-16 только **−0.000006**, residual scale 0/0 и Δ≈0. **NONE; закрыть tuning probe/features/mapping/scales.** Следующая отдельная ставка — BURST-STATE representation, не реализована.
- **Fixed threshold-3 BURST/GAP episode representation** (`exp_054`): structural novelty PASS, но mid-activity `P(y>0)` spread только **0.01998** (<0.03), AUC REAL−SHUFFLED **+0.000021** (<0.002), residual donor scales **0/0**, late Δ **0**, REAL−SHUFFLED Δ **0**. **NO_GO_PREFLIGHT; GPU/full folds NO.** Не спасать threshold sweep, episode summaries, architecture или blend weights в этой exact BURST/GAP ветке.
- **Retrospective landmark outcome memory против matched SHUFFLED outcomes** (`exp_055`): exact cutoff/leakage/shuffle audits PASS, но REAL/SHUFFLED residual scales **0/0**, late Δ **0**, REAL−SHUFFLED **0**, обе recipient halves tie; pooled partial residual corr **0.00581<0.02**. **NO_GO_PREFLIGHT; GPU/pilot/full folds NO.** Raw same-user target corr 0.498 не является incremental utility; не спасать stride/window/lags, state vector, shuffle strata, scales или architecture в этой exact ветке.
- **Late input-only masked ETX adaptation против clean-corridor SSL control** (`exp_056`): domain shift real (A→LATE AUC **0.8972**), reconstruction улучшается, но `LATE−CONTROL` cal **+0.000032**, slot **+0.000007**, halves **+0.000058/+0.000005**, clean holdout хуже и MMD не снижается. **REJECT; full folds NO.** Не спасать exact one-epoch ветку mask/lambda/epoch/head sweep.
- **Dataset/user fingerprint против incidence-matched joint PERM** (`exp_058`): integrity/BASE/marginal/test-metadata audits PASS; standalone REAL−PERM **−0.000130**, но в фиксированном слоте **+0.000071**, hash-halves **+0.000069/+0.000073**, positive RMSLE **+0.000206**. **REJECT; full folds/test/LB NO.** Не спасать subsets/buckets/hash/CatBoost/tuning в этой exact fingerprint-ветке.
- **Target-free production-state reweighting fixed UNC slot** (`exp_057`): support честный (domain AUC **0.7213**, ESS **79.3%**, tiny-stratum dominance false), но STATE−SHUFFLED standalone **+0.000254**, fixed slot **+0.000002**, к `STRONGEST_CURRENT` **+0.000016**; user halves **−0.000082/+0.000086**. **REJECT; full folds/test/LB NO.** Не спасать semantic subsets, domain-model/clipping/shuffle-bin tuning, rounds/seeds или segment gates в этой exact ветке.
- **Unresolved Search/Cart after last purchase** (`exp_061`, OPEN-FUNNEL): cutoff/alignment/shuffle audits PASS; raw residual corr до **|0.0223|**, но REAL/SHUFFLED/CONTROL выбрали scale **0/0 на всех 4 folds и обеих user-halves**, `Delta wCV=0`, late `0`, REAL−SHUFFLED `0`. **REJECT; model/test/LB NO.** Не спасать окнами, thresholds, learner или scale tuning эту exact family.
- **Platform-relative daily activity** (`exp_062`, PLATFORM-DETREND): cutoff/current-panel/alignment/factor-marginal audits PASS, placebo меняет **95.56%** привязок дат при max marginal diff `1.4e-15`; REAL/PLACEBO/CONTROL выбрали scale **0/0 на всех 4 folds и обеих halves**, `Delta wCV=0`, REAL−PLACEBO `0`. **REJECT; full model/test/LB NO.** Не спасать factor/window/bin/model tuning эту exact family.
- **Two-part occurrence member против exact exp_037** (`exp_063`): provenance/base replay PASS; E11 nested LOFO **+0.0000105, 1/4**, latest alpha `0`; direct E10 control alpha `0/0/0/0`; лучший fixed E11 `.05` лишь **−0.0000098**. **REJECT; test/LB NO.** Direct occurrence integration закрыта; не спасать соседними весами, retraining, segment gates или public-LB calibration.
- **Explicit daily funnel order** (`exp_064`): cutoff/alignment/state-multiset audits PASS, placebo меняет **97.62–97.77%** feature rows при exact transition-count parity; REAL/SHUFFLED/CONTROL выбрали scale **0/0 на всех 4 folds и обеих halves**, `Delta wCV=0`. **REJECT; full model/test/LB NO.** Не спасать state alphabet/window/lag/learner/segment/neural tuning эту exact family.
- **LATEST-DELTA-COMPATIBILITY без canonical OOF `latest`** (`exp_066`): `AUTHORITATIVE-LATEST-INTEGRATION` отсутствует; среди 627 NPZ и 83 Parquet нет `z_latest`. **BLOCKED_NO_CANONICAL_LATEST_OOF**; не восстанавливать OOF из test components/public LB и не регистрировать native-fold диагностику как project wCV.
- **Pinned `team-b-B2` four-model tabular pipeline как raw-GMV член `STRONGEST-CURRENT`** (`exp_069`): standalone **1.750957** против 1.747510; optimum `w_our=0.83514` дал full **−0.000152, 3/4**, честный LOFO **−0.000151, 3/4**, 09-04 хуже, test/OOF `Var(Δz)` **1.278×**. **REJECT как чемпион: ниже gate/public noise; submission только PREPARED.** Не спасать соседними весами, поздними загрязнёнными cutoff'ами или public-LB tuning.
- **Полная замена 55%-го tab-slot на `team-b-B2`** (`exp_070`): native log **+0.000191, хуже 3/4**, raw **+0.000301**. Частичный optimum исчерпан: `44.96%` team внутри слота (абс. `.24727`) даёт full **−0.000382, 4/4**, LOFO **−0.000381, 4/4**, regime **1.022× PASS**, но ниже gate 0.0005. **FULL REJECT; PARTIAL development/noise**; submission собран только по явному запросу (`exp_071`). Не тюнить соседние веса и не выдавать in-sample optimum за нового чемпиона.
## Последние эксперименты (макс 10 строк, переполнение → HISTORY.md)

| ID | Дата | Автор | Гипотеза | wCV | Вердикт |
|----|------|-------|----------|-----|---------|
| FINAL-TEAM-B-ENSEMBLE | 2026-08-29 | A1 | EXP-071: requested финальная partial slot-интеграция STRONGEST + pinned team B2 | full **−0.000382**; LOFO **−0.000381**, 4/4; regime **1.022×** | **FINAL PREPARED; anchor unchanged** (`exp_071`) |
| TEAM-B-TAB-SLOT | 2026-08-27 | A1 | EXP-070: полная и оптимальная частичная замена 55%-го tab-slot на pinned team B2 | full **+0.000191**; partial LOFO **−0.000381**, 4/4 | **FULL REJECT; partial below gate** (`exp_070`) |
| TEAM-B-B2-ENSEMBLE | 2026-08-27 | A1 | EXP-069: pinned four-model team pipeline как raw-GMV член exact `STRONGEST-CURRENT` | full **−0.000152**; LOFO **−0.000151**, 3/4 | **PREPARED submission; REJECT promotion** (`exp_069`) |
| AUTHORITATIVE-LATEST | 2026-08-25 | A1 | EXP-067: exact TEST rebuild + LB/CAP/canonical-OOF provenance | TEST error **8.88e-16**; latest OOF missing | **CONTINUE_PROVENANCE; anchors split** (`exp_067`) |
| LATEST-DELTA-COMPAT | 2026-08-25 | A1 | EXP-066: SAFE/BTYD05/FRESH/SEQ65 residual после смены базы на `latest` | primary **NOT RUN**; canonical `z_latest` absent | **BLOCKED_NO_CANONICAL_LATEST_OOF** (`exp_066`) |
| FINAL-INTEGRATION | 2026-08-25 | A1 | EXP-065: independent exp037 rebuild + exactly two checked candidates | A exact **1.747510**; regime **0.775×** | **ACCEPT package; strongest unchanged; LB NO** (`exp_065`) |
| EVENT-ORDER | 2026-08-25 | A1 | EXP-064: daily Search/Cart/Buy transitions против within-user state-order shuffle | REAL/SHUF/CONTROL **0**, 0/4 | **REJECT; scale 0/0 обеих halves** (`exp_064`) |
| OCCURRENCE-REVISIT | 2026-08-25 | A1 | EXP-063: two-part E11 против exact champion и same-feature direct E10 control | nested **+0.0000105, 1/4**, latest alpha 0 | **REJECT; occurrence direct integration closed** (`exp_063`) |
| PLATFORM-DETREND | 2026-08-25 | A1 | EXP-062: user activity относительно same-day current-panel factors против date-shuffled placebo | REAL/PLACEBO/CONTROL **0**, 0/4 | **REJECT; scale 0/0 обеих halves** (`exp_062`) |
| OPEN-FUNNEL | 2026-08-25 | A1 | EXP-061: Search/Cart state after last purchase против joint within-state shuffle | REAL/SHUF/CONTROL **0**, 0/4 | **REJECT; scale 0/0 обеих halves** (`exp_061`) |
LB: **STRONGEST-CURRENT 1.6496571** < SEQ-01-MIX 1.6501764 < DIST-MIX 1.6507774 < MIX-E11 1.6510029 < f4 1.6512012 < S1-BEST 1.6512803 < **SEQAVG3-MIX 1.6553136** < S2 1.6619325 < MIN 1.6674246 < SIM 1.6682180. `L*=2.3293`.
## Backlog — см. `PROBABLY_EXP.md` и `experiments/README.md`

- [x] **ЗАКРЫТЫ:** **030** (`exp_030c`) ΔwCV −0.00095 ± 0.00009, 3/4; **032/032B** свежесть −0.00101…−0.00128 4/4, гибрид в смесь не идёт; **035** LOFO слота + сабмит, шум тестовой модели `Var=0.035` = 72% смены сида; **036** `ETX` как ЗАМЕНА REJECT 2/4, как СОАВТОР — лучший член слота, выигрыш на КРАЯХ активности (никогда не покупал ΔAUC +0.0276) и в ДЛИННОЙ истории.
- [x] **EXP-037** ЗАКРЫТ: блокер `ETX` снят и ОБЪЯСНЁН, 3.22×→**0.78×**; `ETX-AVG3` wCV **1.74861**, сид-шум 0.00009; сегментный гейт **REJECT**. **EXP-038/039/040/044** ЗАКРЫТЫ: funnel **REJECT**; BLOCK4-SAF **+0.00024, 0/4**; FRESH-CONTRAST **−0.000225, 4/4**; paired FT **−0.000088, 3/3**, ниже gate.
- **Decision / open IDs:** узкое место — **интенсив SEQ** (+0.00257 к `μ_DIST`, 0/4); `COND-FRESH`, neural FT, tabular refresh и classic BTYD закрыты (`exp_040/044/046/047`). Открыты **031** `SEQ-D3C`, **033** `S06`, **034** `S12`.
