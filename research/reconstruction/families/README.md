# Research families

The 12 families here were consolidated bottom-up from observed changes, models,
targets, and artifacts. They are descriptive rather than a prescribed project
taxonomy.

Each page reports positive/negative/inconclusive counts and best/median values
only inside a single `comparison_class`. A family with several negative
implementations is not declared universally impossible; saturation wording is
explicitly implementation- and protocol-scoped.
