# STATE — читать целиком перед каждой задачей (лимит ~80 строк)

## Текущий лучший

**`SEQ-01-MIX`** (`exp_025`) — **LB public 1.6501764**, wCV **1.74834**: 0.15·NORM + 0.20·UNC +
0.10·CAP + 0.25·DIST + 0.30·SEQ-01, level 2.3293, `--depth-clip 289`. Было `S1-DIST-MIX` 1.6507774.
`SEQAVG3-MIX` full365 = 1.6553136 (`exp_027`); `SEQ-AVAIL-AUG` (`exp_029`) **REJECT**: OOD снят, fold хуже.

## Метрика и валидация — ЕДИНАЯ схема, `exp_016`

Метрика — RMSLE в `log1p`; главная **wCV**: пофолдовая калибровка, веса 1:2:4:8 (`exp_016`;
у SEQ перенос на LB 0.57). Фолды 09-04/09-18/10-02/10-16; val-панель 3-блочная,
train-коридор 04-03..10-16, шаг 7, `T+30≤V`. **Пороги:** Δ≤−0.0020 → отправлять;
−0.0020…−0.0005 → разработка; выше — шум; ≥3/4, включая 10-16. Public noise 0.00025,
пол 0.0005. Веса — LOFO, E03a ненулевой.
Seed std: GBDT 0.00010, **TCN 0.00250**; пол разнообразия `Var(Δ)=0.00712`.

## Не повторять (провалы; список только растёт, ничего не удалять)

- **Cutoff 2026-01-14 и любой T≥2025-10-17** отравлены гарантированным окном панели (`e08`; повторно подтверждено аудитом `exp_028`, FRESH-DIST-MIX остановлен до обучения).
- **3-блочная train-панель** (`S1-E01`) хуже +0.00102, 4/4; val-панель обязана быть 3-блочной.
- **×1.16** (`e05`), **user_id** (`e04`), **сырые недельные лаги** (`e15`), **минус длинные фичи** (`S1-E04`) — ноль/хуже.
- **L=90** (`E03c`) и **строгий коридор L=180** (`E03b`): выбор L по минимуму bias ведёт к худшей модели.
- **EXP-MIN** (+0.01614) и **EXP-SIM** (+0.01694): низкая corr прогнозов ≠ полезная декорреляция ошибок.
- **Дельты со слабой базы не переносятся**: `DIST` дал −0.0040 против 3 cutoff'ов и −0.0015 против плотной.
- **Только выборка последнего fold** (`DIST-F4`): LB +0.00042; локальный валидатор слеп.
- **«Асимметрия 0.64×»** отменена (`exp_016`): артефакт OOF cal, работает лишь на крупных дельтах.
- **Rounds≥450 у direct** (`exp_017`): 600 переобучает, 1600 стоят +0.00287, 0/4. Capacity зависит от объёма train.
- **Подбор весов ради 4-го знака** (`MIX-E11`): wCV −0.00038, LB +0.00023; E03a нельзя обнулять.
- **Gap-axis как selection gate** (`exp_019`): bias slope не воспроизведён, вывод зависит от k; только диагностика.
- **`train_blocks=0`** (`exp_020`): avg3 +0.000113, 2/4; +9.23% строк без выигрыша.
- **Вся постобработка поверх `ẑ`** (`rmsle_diagnostics`): аффинная −0.00027, посегментная
  −0.00017, правила по `rec_buy` — знак неустойчив. Закрыто, включая `EXP-17`/`EXP-18`.
- **Личное время как представление** (`exp_021`, S_08): 30 признаков `pt_*` → −0.000006 wCV,
  ΔAUC ≈ 0. Контроль с ρ, **перемешанной между людьми**, не хуже настоящей → сигнала нет.
- **Dense supervision при равном объёме** (`exp_022`, S_02B): +0.001263 wCV, 0/4; AUC −0.000416.
- **Multi-horizon hazard + count** (`exp_024`, S_03): +0.00286, 0/4; `FULL` 1.75234 = контроль
  `SELF` 1.75235 — стекинг без новой информации, `C`/`C2` закрыты. **Стекинг мерить от `SELF`,
  а не от базы: приём сам стоит +0.0004 wCV.**
- **TCN как источник РАНЖИРОВАНИЯ** (`exp_025`/`exp_026`, S_10): даже усреднение 3 сидов не
  поднимает AUC(y>0) выше смеси. Энкодер ценен **только** как член смеси (см. Backlog).
- **Полная глубина 365 на тесте у TCN** (`exp_027`, LB +0.0051): «не резать» верно для
  валидации и НЕВЕРНО для теста. У всех 29 обучающих cutoff'ов и 4 фолдов в окне ≥76 позиций
  `avail=0`, на тесте при полной глубине их **0** — режим без обучающих примеров, на истории
  невоспроизводимый; цена бита **+0.0034…+0.0044** против −0.0037 у самих данных. **На тесте
  всегда `--depth-clip 289`**; `α>0` подмешивает тот же режим (`α*=0.75` 7/7 LOFO переносу НЕ
  подлежит), гейтинг отклонён. **OOF/LOFO живут при `avail` с нулями = это прогноз для сабмита
  С ОБРЕЗКОЙ.**
- **Train-time `avail` augmentation** (`exp_029`): `avail_bnd`/`avail_drop p=.25` сняли `avail≡1` penalty
  (+0.00651 → ≈0), но дали +0.00178/+0.00105 RMSLE; gain +77д сжался −0.00308 → −0.00165/−0.00206. Не продолжать; production = `clip289`.

## Последние эксперименты (макс 10 строк, переполнение → HISTORY.md)

| ID | Дата | Автор | Гипотеза | wCV | Вердикт |
|----|------|-------|----------|-----|---------|
| SEQ-AVAIL-AUG | 2026-08-14 | A1 | train-only сдвиг границы `avail`, включая `avail≡1` | F4 +0.00105 best | **REJECT** (`exp_029`) |
| FRESH-DIST-MIX | 2026-08-13 | A2 | +13 физически размеченных, но selection-contaminated cutoff'ов | — | **REJECT audit** (`exp_028`) |
| SEQ-DEPTH | 2026-08-13 | A1 | разбор провала LB: кросс-фолдовый стресс глубины + цена ухода `avail` | — | **ACCEPT** (`exp_027`) |
| SEQ-AVG3 | 2026-08-13 | A1 | S_10: усреднение 3 сидов TCN + диагностика глубины истории | **1.74963** | ACCEPT приём, но **LB 1.6553136** |
| SEQ-01 | 2026-08-13 | A1 | S_10 B: dilated TCN на сырой дневной истории 365д вместо 227 агрегатов | 1.75270 | **CONTINUE** (`exp_025`) |
| MHZ-FULL | 2026-08-13 | A1 | S_03: multi-horizon hazard + счёт как супервизия | 1.75234 | **REJECT** (`exp_024`) |
| HOLIDAY-YOY | 2026-08-12 | A1 | персональная holiday-response 2025→2026 + placebo | 1.74958 (+0.00009) | **SEND_HIGH_RISK** (`exp_023`) |
| S1-DIST-MIX | 2026-08-11 | A1 | смесь с головой распределения | **1.74948** | **LB 1.6507774** |
| S1-ROUNDS | 2026-08-12 | A1 | S_05 A: кривая по раундам `direct`, 25..1600 | **1.75108** | в разработку; 600 = переобучение |
| S1-SEEDAVG5 | 2026-08-12 | A1 | S_05 B: усреднение 5 сидов при 300 раундах | **1.75037** | в разработку; брать 3 сида |

LB: SEQ-01-MIX 1.6501764 < DIST-MIX 1.6507774 < MIX-E11 1.6510029 < f4 1.6512012 < S1-BEST
1.6512803 < **SEQAVG3-MIX 1.6553136** < S2 1.6619325 < MIN 1.6674246 < SIM 1.6682180. `L*=2.3293`.

## Backlog

- [ ] **`S10` (SEQ):** простая `avail` augmentation закрыта (`exp_029`); production остаётся
  `clip289`. Следующий независимый рычаг — `SEQ-03` (ёмкость), только новым gate. Усреднение
  исчерпано; `AVG3 @ clip289` LOFO −0.00055 к отправленному, но ниже порога submit.
- [ ] Порядок: `research/strategies/STRATEGIES_INDEX.md`; Tier A — `results/TIER_A_REPORT.md`.
  S05 A+B PARTIAL, S01/S02A/S03 REJECT, S02B/S08 FAIL. Осталась проверка S04.
  `STRATEGY_13` (ETX) DEPRIORITIZED (`exp_021`): гейт S08 не пройден, прироста AUC у `S10` нет.
- [ ] Диагноз ошибки — `research/rmsle_diagnostics/README.md`: 74% дисперсии — «попадёт ли
  покупка в окно», 70.6% ошибки в полосе 2–15 дней покупок за 180д, AUC(y>0)=0.844 — потолок
  признаков; +0.001 AUC ≈ −0.0006…−0.0019 wCV. Осталось: S04 (120 дней) либо ёмкость энкодера.
