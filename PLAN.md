# PLAN — исследовательская программа: где вычерпывать 0.01–0.02 RMSLE

> Написан по результатам аудита (artifacts/audit/audit_report.md) и 33 экспериментов.
> Исполнителю: это план ИССЛЕДОВАНИЯ с приоритетами и ожиданиями, а не список обещаний.
> Сначала прочти STATE.md и этот файл целиком. Правила из AGENTS.md и раздел 8 этого
> файла — жёсткие.

---

## 1. Где мы стоим (проверенные цифры)

- Baseline: LightGBM, 50 признаков, single-cutoff, δ=0.
  fold1=1.690608, fold2=1.743426, fold3=1.732506, **mean(1,2)=1.717017**, LB=1.6615.
- LB-пробы: δ=0→1.6615; H (spring multi-cutoff)→**1.6599**; G_hurdle (заявл.)→1.6562.
- Топ-1 LB ≈ 1.648. Offset LB ≈ local − 0.056.
- ~33 гипотезы проверены. **Ни одна не дала локально ≥ 0.005.** Лучшие крохи:
  CatBoost-blend +0.00096, seed bagging +0.00065, XGB-blend +0.00059, hurdle +0.00077.
- В коде сейчас δ=0; рабочее дерево = 79 признаков (50 baseline + 28 lag + var_predictability).
  Базовые 50 признаков — из `git show HEAD:src/features.py`. Кэш датасетов на 29 cutoff'ов валиден.

## 2. Бюджет ошибки (где лежит масса SLE, fold1)

| Сегмент | Доля SLE | Bias | Что это значит |
|---|---|---|---|
| y=0 (46% юзеров) | 51.7% | −1.48 | перепрогноз неактивных |
| y>0 | 48.3% | +0.83 | недопрогноз покупателей |
| rec_buy 15–60 дней | 41.9% | −0.26 | «уснувшие»: главный резерв |
| w365_gmv deciles d4–d6 | ~38% | −0.24…−0.28 | перепрогноз «середины» |
| киты (top 0.1%) | 0.3% | +2.73 | недопрогноз, но масса мала |

Ключевой факт: модель **систематически перепрогнозирует** все сегменты, кроме китов.
Это следствие сезонного сдвига (признаки из «сильного» месяца → таргет «слабого»).

## 3. Главный вывод ресёрча (честно)

**Потолок single-model GBDT на текущих признаках достигнут.** 33 суб-шумовые попытки
(признаки, лоссы, тюнинг, ансамбли, multi-cutoff, hurdle) — это статистика, а не неудача.
Локальный выигрыш 0.01–0.02 ОДНОЙ идеей почти наверняка не существует. Реалистичная стратегия:

1. **Направление A «Комбайн крох»** — локально измеримые мелкие эффекты, которые СКЛАДЫВАЮТСЯ
   (дисперсия моделей слабо коррелирована): ожидание суммарно 0.003–0.007.
2. **Направление B «Сезонный перенос»** — локально НЕ измеримо в принципе (нет весеннего
   val-окна), но именно там лежит LB: уже подтверждено H=1.6599 и (заявлено) 1.6562.
   Лимит сабмитов расширен → LB-пробы становятся дешёвым измерительным инструментом.
3. **Направление C «Структурные признаки»** — 2–3 неперепробованные идеи с реальным механизмом.

## 4. Направление A — комбайн крох (каждый пункт = 1 итерация)

A1. **Temporal ensemble.** Усреднение в log-пространстве предсказаний single-cutoff моделей,
    обученных на 3–4 соседних cutoff'ах (fold1: 2025-11-17, 12-01, 12-15; аналогично сдвигая
    для fold2/fold3). Отличается от multi-cutoff обучения (exp_031, reject): модели не
    смешивают сезоны в одном трейне, а голосуют. Кэш готов. Ожидание: −0.001…−0.003.
A2. **Seed bagging ×5 поверх лучшего конфига** (exp_013 дал +0.00065 в одиночку). Ожидание: −0.0005.
A3. **Гетерогенный бленд** LGBM + CatBoost (+ XGB): крохи exp_023/+0.00096 и exp_024/+0.00059
    частично независимы. Веса — сетка 0.1, проверка стабильности на всех 3 фолдах. Ожидание: −0.001…−0.002.
A4. **Финал направления:** принять только если сумма ≥ 0.005 по правилу accept; иначе зафиксировать
    «локальный потолок = шум» и сосредоточиться на B.

## 5. Направление B — сезонный перенос (только LB-программа)

Предыстория: тест-окно весеннее (y=0 ≈ 60% против 44–46% на фолдах; YoY-коэффициент окна 1.1678).
Локально это не измерить — все фолды зимние. Значит, двигаемся LB-пробами, строго по одной
переменной, каждую пробу фиксируем в experiments.md (файл, конфиг, скор).

B1. **Сетка весеннего обучения** (развитие exp_033, LB 1.6599):
    - B1a: 8 весенних cutoff'ов (2025-02-14 … 2025-03-29 шаг ~7д) БЕЗ свежих;
    - B1b: 4 весенних + 4 свежих, вес весенних ×2;
    - B1c: 2 весенних (02-14, 03-15) + 6 свежих.
    Гипотеза: доля весенних данных в трейне — главная переменная.
B2. **Бленд в log-пространстве**: spring-модель (exp_033) × baseline δ=0, веса 0.3/0.5/0.7.
    Дёшево: обе модели уже обучены, нужны только файлы.
B3. **Сезонный коэффициент поверх spring-модели** (аналог exp_030: ×1.1678 и ×1.12 в рублях).
    exp_030 уже показал на hurdle, что это работает на LB (1.6567/1.6562). Проверить,
    складывается ли с весенним обучением или это тот же эффект.
B4. **Защита от LB-переобучения:** не более 1 переменной на пробу; все скоры в журнал;
    финальные 2 сабмита выбираем среди конфигов, улучшившихся на ДВУХ независимых пробах
    (или 1 проба + сильный локальный механизм). G_hurdle (1.6562) получен подбором 1.12
    по LB — считать недоверенным до независимого подтверждения.

## 6. Направление C — структурные признаки (локально измеримо, есть механизм)

C1. **Return-rate target encoding** (главный локальный резерв: сегмент rec_buy 15–60 = 42% SLE).
    Идея: по ОБУЧАЮЩИМ cutoff'ам (не по val!) посчитать эмпирические
    `P(y>0)` и `E[log1p(y) | y>0]` в бакетах (rec_buy_бакет × rec_over_buygap_бакет × w90_orders_бакет)
    и дать их как 2 признака. GBDT учит это же, но неявно и в пределах одного cutoff'а;
    явное сглаживание по 8+ cutoff'ам снижает дисперсию оценки. Дисциплина: статистики только
    из таргетов обучающих cutoff'ов; для val/test — те же бакеты, маппинг без пересчёта.
    Ожидание: −0.001…−0.003, проверять отдельно на каждом фолде.
C2. **Weekend-пакет** (4–6 колонок): доля gmv/заказов/активных дней в выходные за 30/90 дней.
    В брифе фигурировал в legacy-группах, в текущем пайплайне отсутствует. Ожидание: −0.0005…−0.002.
C3. **Platform-trend признаки внутри multi-cutoff**: gmv платформы за 7/30 дней до cutoff и их
    отношение. В single-cutoff константны (exp_003 reject справедливо), в multi-cutoff варьируются
    между строками и могут научить модель поправке на сезон. Связка с exp_031-инфраструктурой.
    Ожидание: неизвестно, это единственный способ дать модели «сезонный контекст» локально.

## 7. Что НЕ делать (закрыто аудитом/экспериментами)

- Глобальный δ: fold1=−0.23, fold2=−0.10, fold3=+0.05, тест=0. Непереносимо, тема закрыта.
- SPLY-признаки: локально не измеримы (покрытие окон 0/13/30 дней), standalone сигнал слабый
  (corr 0.41, хуже naive30). Не тратить итерации.
- Hurdle в любых формах: рублёвый баг exp_006 исправлен в exp_028 — sub-noise. Закрыто.
- Дельта-моделирование (exp_032): хуже на всех фолдах. Закрыто.
- Простой multi-cutoff без сезонных признаков (exp_005, exp_031): шум. Закрыто.
- Тюнинг дальше 6 точек G15: убывающая отдача, всё sub-noise.

## 8. Протокол итерации (не менять)

1. Одна итерация = одно изменение; 3 фолда; seed из config.py.
2. accept ⇔ mean(1,2,3) лучше ≥0.005 И каждый фолд лучше ≥0.002; 0.002–0.005 → второй seed.
3. Любые константы (веса блендов, пороги): знак и порядок оптимумов обязаны совпадать на 3 фолдах.
4. Error-анализ fold1 (y=0/y>0, deciles, rec_buy, tenure, киты) — каждую итерацию.
5. Запись в experiments.md + STATE.md до перехода к следующей итерации.
6. Приоритет фолдов при споре: fold1 наиболее похож на тест (post-holiday окно).
7. Датасеты кэшировать; новые признаки — только через build_features, новыми колонками в конец.
8. LB-пробы: файл + конфиг + скор в experiments.md; две пробы на одну переменную максимум.

## 9. Ожидаемый итог и stop-условия

- Направление A: честные −0.003…−0.007 к 1.717017 (≈ 1.710–1.714). Это реалистичный локальный максимум.
- Направление B: потенциально LB 1.655–1.660 (по факту проб), локально невидимо.
- Цель «local ≤ 1.707» (−0.01) достижима только если A4 + C1 дадут верхние границы ожиданий
  И сложатся. Если после A1–A4 + C1–C3 сумма < 0.005 — СТОП, фиксируем потолок,
  оставшиеся сабмит-дни тратим на B-программу и выбор двух финалов из подтверждённых конфигов.
