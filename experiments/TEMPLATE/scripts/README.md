The default executable source is the repository-level
`scripts/run_tabular_experiment.py`. Add experiment-local scripts only when the
root runner cannot express the exact hypothesis; keep reusable code under `src/`.
