# STATE — читать целиком перед каждой задачей (лимит ~80 строк)

## Текущий лучший

**`S1-DIST-MIX`** (`exp_014`) — **LB (public) 1.6507774106**, лучший результат и
**финальный ответ**. Смесь 0.15·S1-E10 + 0.30·S1-E02 + 0.10·S1-E03a + **0.45·S1-DIST**,
`--level 2.3293`, **wCV 1.74948**, сабмит `submissions/submission_dist_head.csv`
(описание — `README.md` §Решение).

**Сабмитов не осталось.** Последний (`S1-MIX-E11`) отправлен вопреки вердикту `exp_016`
§7 и дал 1.6510029 — +0.00023 при обещанных −0.00038; порог `Δ wCV ≤ −0.0020` подтверждён.

## Метрика и валидация — ЕДИНАЯ схема, `exp_016`

Метрика соревнования: **RMSLE** (`log1p`, отрицательные клипятся нулём). Главная
метрика решений — **`wCV`**: взвешенное **1:2:4:8** среднее пофолдовых RMSLE **после
оптимального лог-сдвига** (`src/report.py`, `FOLD_WEIGHTS_S1`). Калиброванная — уровень
ставится измеренным якорём; взвешенная — перенос на LB **1.000 ± 0.057** против ±0.151
у равных весов и ±0.240 у OOF cal. Каждый прогон пишет `artifacts/report_<exp>.json`
и строку в `log.csv`; реестр сабмитов — `submissions.csv`, связь с LB — `python -m src.cv_lb`.

Схема фолдов не менялась: out-of-time cutoff'ы, `V ∈ {2025-09-04, 2025-09-18,
2025-10-02, 2025-10-16}`, панель валидации **всегда 3-блочная**, train-cutoff'ы
`T + 30 ≤ V`, чистый коридор **2025-04-03 .. 2025-10-16**, шаг 7 дней.

**Пороги.** Парный шум public LB = **0.00025** (не 0.0058: сабмиты считаются на одних
и тех же 50k, сравнение парное); локальный парный шум на OOF — 0.00006. `Δ wCV ≤ −0.0020`
→ отправлять; `−0.0020…−0.0005` → держать в разработке; `> −0.0005` → шум. Плюс: лучше
на **≥3 фолдах из 4, включая 10-16**. Веса смеси — только через `python -m src.blend
--lofo` и только при **фиксированной** ненулевой доле страховки `S1-E03a`: её ценность
валидатору не видна, и перебор её обнуляет.

**Пол разрешения — 0.0005, а не 0.00006** (`exp_016` §8): смещение неизмеримой оси
train→test не усредняется и перекрывает четвёртый знак — на дельте −0.00038 перенос
вышел **−0.60** вместо ~1.0, тогда как дельты ≥0.002 переносятся 0.95–1.08.

**Шум сида — std(wCV) 0.00010**, разница двух одиночных прогонов 0.00014 (`exp_018`):
Δ < 0.0003 на одиночных сидах — не результат, 0.0003–0.0010 мерить на 3-сидовых
средних. **Пол разнообразия — `Var(Δ) = 0.00712`**, corr остатков 0.99885 (два сида).

## Не повторять (провалы; список только растёт, ничего не удалять)

- **Cutoff `2026-01-14` и любой `T ≥ 2025-10-17`** — отравлены: target-окно лежит
  внутри гарантированного окна активности панели (`e08`).
- **Переприменение 3-блочной панели на ОБУЧАЮЩИХ cutoff'ах** (`S1-E01`): +0.00102,
  4 фолда из 4. Панель **валидации** обязана оставаться 3-блочной.
- **Мультипликативная поправка ×1.16** (`e05`), **`user_id`** (`e04`), **сырые
  недельные лаги** (`e15`), **удаление длинных признаков** (`S1-E04`) — ноль или хуже.
- **Усечение истории до L=90** (`S1-E03c`) и **строгий коридор под L=180** (`S1-E03b`):
  правило «выбирать L по минимуму смещения» ведёт к худшей конфигурации.
- **Radical feature minimalism** (`EXP-MIN`, +0.01614) и **rank-cohort similarity**
  (`EXP-SIM`, +0.01694): низкая корреляция предсказаний ≠ полезная декорреляция ошибок.
- **Дельты со слабой базы к боевой не переносятся** (`S1-DIST`): голова
  распределения давала −0.0040 против базы из 3 cutoff'ов и −0.0015 против плотной.
- **Обучение только на выборке последнего фолда** (`S1-DIST-F4`, `exp_015`): LB
  **+0.00042**. И отдельно — **валидатор слеп к таким изменениям**: обучающая выборка
  фолда 10-16 и есть эти 24 cutoff'а, CV для 24 и 29 cutoff'ов одинаков. Всё, что
  трогает обучающую выборку ближе к тесту, чем на 30 дней, локально не проверяется.
- **«Асимметрия переноса 0.64×»** — отменена (`exp_016`): артефакт `OOF cal`, на `wCV`
  тот же замер даёт 0.975. Перенос равен 1.0 — **но только на крупных дельтах**, ↓.
- **Число раундов ≥450 у `direct`** (`exp_017`): 600 — переобучение, а не недобор
  (`strategy_1_results.md` §9 п.2 обещал обратное); 1600 стоят **+0.00287 wCV**, 0/4
  фолда. Оптимум 150–300 и растёт с объёмом выборки (+50 на 2 cutoff'а) — переносить
  число раундов между конфигурациями с разным объёмом нельзя.
- **Подбор весов смеси ради дельты четвёртого знака** (`S1-MIX-E11`): wCV −0.00038,
  4 фолда из 4, LOFO-честно −0.00036 — и **+0.00023 на LB**, перенос −0.60. Оптимум
  по wCV обнуляет `S1-E03a`, страховку против 120-дневной экстраполяции: выигрыш
  подбора берётся из измеримого пространства, плата (~0.0005) — из неизмеримого.

## Последние эксперименты (макс 10 строк, переполнение → HISTORY.md)

| ID | Дата | Автор | Гипотеза | wCV | Вердикт |
|----|------|-------|----------|-----|---------|
| S1-E11 | 2026-08-10 | A1 | двухчастная модель на признаках S1-E10 | **1.75070** | KEEP; в смеси не помог |
| S1-DIST | 2026-08-11 | A1 | голова распределения: 16 бинов z вместо L2 (E0) | **1.75062** | KEEP |
| S1-DIST-MIX | 2026-08-11 | A1 | смесь с головой распределения | **1.74948** | **LB 1.6507774** |
| S1-DIST-F4 | 2026-08-11 | A1 | обучение только на выборке фолда 10-16 | неизмеримо | REJECT; LB 1.6512012 |
| S1-VAL-W | 2026-08-11 | A1 | единая схема валидации wCV, калибровка по LB | — | ПРИНЯТО (`exp_016`) |
| S1-MIX-E11 | 2026-08-11 | A1 | перебор весов смеси по wCV (+E11, −E03a) | 1.74911 | REJECT; LB 1.6510029 |
| S1-ROUNDS | 2026-08-12 | A1 | S_05 A: кривая по раундам `direct`, 25..1600 | **1.75108** | в разработку; 600 = переобучение |
| S1-SEEDAVG5 | 2026-08-12 | A1 | S_05 B: усреднение 5 сидов при 300 раундах | **1.75037** | в разработку; брать 3 сида |

Замеры LB (public): **S1-DIST-MIX 1.6507774106** < S1-MIX-E11 1.6510029 < f4 1.6512012 <
S1-BEST 1.6512803 (level-free 1.6512512) < S2-BEST 1.6619325 < EXP-MIN 1.6674246 < EXP-SIM
1.6682180. Уровневые S1-BEST: `low` 1.6519790, `high` 1.6529909 → `L* = 2.3293`,
`dm = +0.0872`. CV и LB несопоставимы по абсолюту (LB лучше на 0.106).

## Backlog

- [ ] **Бэклог стратегий и порядок запуска: `research/strategies/STRATEGIES_INDEX.md`**
  (аудит `LIST_EXPERIMENTS.md`, 13 карточек, что отвергнуто и почему).
  **`S_05` A+B выполнен** (`exp_017`, `exp_018`, итоги — `research/strategies/results/STRATEGY_05/`).
  Дальше: `S_05` C/D (раунды для `dist`; 3 сида и 300 раундов у всех членов смеси
  при **фиксированных** весах), вторая ось валидации `S_01`, `train_blocks=0` `S_02`
  (в каждом плече снимать мини-кривую по раундам); HDN `S_10` — после гейта.
